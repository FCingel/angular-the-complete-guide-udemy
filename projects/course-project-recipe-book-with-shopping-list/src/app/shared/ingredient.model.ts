export class Ingredient {
    // Shorthand for commented below code for creating properties and assigning them in the constructor
    constructor(public name: string, public amount: number) {}

    // public name: string;
    // public amount: number;

    // constructor(name: string, amount: number) {
    //     this.name = name;
    //     this.amount = amount;
    // }

}